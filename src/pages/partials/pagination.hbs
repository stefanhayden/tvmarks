{{#with pagination}}
  <div class="pagination">
    {{#if hasPreviousPage }}
      <a href="{{url}}?offset={{previousOffset}}">
        Previous
      </a>
    {{else}}
      <span>
        Previous
      </span>
    {{/if}}
    <span class="status">
      Page {{currentPage}}
    </span>
    {{#if hasNextPage}}
      <a href="{{url}}?offset={{nextOffset}}">
        Next
      </a>
    {{else}}
      <span>
        Next
      </span>
    {{/if}}
  </div>
{{/with}}
